const GRID_ROWS = 3;
const GRID_COLS = 3;

const GRID_DIMS = [GRID_ROWS, GRID_COLS];
const all_constants = {GRID_DIMS};

export * from './game-actions';
export * from './user-actions';

export default all_constants;